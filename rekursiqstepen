#include<iostream>
using namespace std;
 int n(int a,int b)
 {
	 if(a==1) return b;
	 else if(a==0) return 1;
	 return b*n(a-1,b);
 }
 int main()
 {
	 int c,d;
	 cout<<"stepen=";
	 cin>>c;
	 cout<<"chislo=";
	 cin>>d;
	 cout<<n(c,d)<<endl;
	 system ("pause");
	 return 0;
 }
